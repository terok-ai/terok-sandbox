# terok-gate-version: {{UNIT_VERSION}}

[Unit]
Description=terok git gate connection handler

[Service]
Type=simple
StandardInput=socket
ExecStart={{TEROK_GATE_BIN}} --inetd --base-path={{GATE_BASE_PATH}} --token-file={{TOKEN_FILE}}
