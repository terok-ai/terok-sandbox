# terok-credential-proxy-version: {{UNIT_VERSION}}

[Unit]
Description=terok credential proxy socket

[Socket]
ListenStream={{SOCKET_PATH}}
SocketMode=0600
ListenStream=127.0.0.1:{{PORT}}

[Install]
WantedBy=sockets.target
